#include <stdio.h>

int main()
{
int a;
scanf("%d", &a);
if ((giulio>18 && giulio<30) || (giulio>60 && giulio<100))
 {
    printf("giulio tu puoi e gli altri sfigati no :)");
 }
if (!((giulio>18 && giulio<30) || (giulio>60 && giulio<100)))
  { 
    printf("giulio vai via tu non puoi mangiare gratis");
  }
  if (!((marta>18 && marta<30) || (marta>60 && marta<100)))
  { 
    printf("marta vai via tu non puoi mangiare gratis");
    if ((marta>18 && marta<30) || (marta>60 && marta<100))
 {
    printf("marta tu puoi e gli altri sfigati no :)");
  }
}